spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/talenteo}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

payroll:
  rules:
    income-tax-rate: 0.15
    social-contribution-rate: 0.09
    high-salary-threshold: 5000.00
    high-salary-surcharge-rate: 0.05

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
